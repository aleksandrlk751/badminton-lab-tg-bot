# Формулы и константы (FORMULAR)

> Единый справочник формул проекта и их дефолтных констант: **где применяется** (локализация в
> коде/конфиге) и **что означает**.
>
> Источник **смысла** формул — [`BRIEF.md`](BRIEF.md) (§3 официальные, §4 метрики, §6 подбор).
> Этот документ фиксирует **локализацию и дефолты**. При изменении формул/констант —
> синхронизируй `BRIEF.md` и этот файл.

## Принципы

1. **Официальные формулы ЛАБ зеркалим as-is** — не заменяем своими (правило проекта, `AGENTS.md`).
2. **Собственные метрики** (S, форма, прогноз, скор) — надстройка сверху; их константы **вынесены в
   конфиг** и подлежат **калибровке позже** (открытый вопрос, `BRIEF.md` §11).
3. Числовые дефолты — [`FORMULAR-CONFIG.md`](FORMULAR-CONFIG.md) и `application-core.yml` / `MetricsProperties`.

---

## 1. Официальные формулы ЛАБ (as-is)

### 1.1 Дельта рейтинга одиночки за матч

```
δ = (100 - (R_поб - R_проигр)) / 10
```

- Если рейтинг победителя ≥ рейтинга проигравшего на **100+** → `δ = 0` (победа ожидаема).
- Проигравший теряет столько же (симметрия). **Пол** рейтинга = 1.

**Локализация:** не пересчитываем. Значение дельты берём из источника при парсинге и сохраняем:
- `TournamentResultsParser` → `ResultUpsertService` → `participation.rating_delta`;
- `TournamentGamesParser` → `MatchUpsertService` → `match_player.rating_delta`
  (для пары дельта **одинакова** для обоих игроков стороны).

**Описание:** знаковая дельта кодирует ожидаемость результата — база для метрики «Форма» (§2.2).

### 1.2 Рейтинг пары

```
R_пара = (A + B) / 2
```

где `A` — сильнейший, `B` — слабейший партнёр. Источник: `content/10`.

**Локализация:** `PairRatingService#pairRating` (`core.metrics`, **реализовано, этап 3**); в подборе
партнёров (§2.5) — `PartnerScoreService` (**реализовано, этап 6 ✓**).

---

## 2. Наши метрики (надстройка)

Обозначения: `Δt_i` — время с i-й встречи; `H` — период полураспада; `δ_i` — знаковая дельта ЛАБ;
`W_max` — потолок веса в начале первого периода; `α` — крутизна спада в `[0, H]`.

**Вес свежести** (общий для §2.1 и §2.2, `MetricMath#decayWeight`):

```
         ⎧ W_max + (0.5 − W_max) · 2 · (1 − 0.5^((Δt/H)^α))     если Δt ≤ H
w(Δt) =  ⎨
         ⎩ 0.5^(Δt/H)                                           если Δt > H
```

При `W_max = 1` и `α = 1` — прежняя кривая `0.5^(Δt/H)` на всём диапазоне.
Якоря `w(H) = 0.5`, `w(2H) = 0.25`, … — без изменений.

### 2.1 Индекс сыгранности S

```
S = Σ_i w(Δt_i)
```

**Константы:** `H`, `W_max`, `α`. **Применяется к** соперникам (H2H) и партнёрам (совместные турниры).
**Локализация:** `PlayabilityIndexService` (`core.metrics`, **реализовано, этап 3**). **Описание:**
«вес» накопленного опыта встреч с затуханием по каждой встрече.

### 2.2 Форма игрока

```
Form = Σ_i δ_i · w(Δt_i)
```

**Константы:** `H`, `W_max`, `α`. **Локализация:** `FormService` (`core.metrics`, **реализовано, этап 3**), вход —
`RatingDeltaEvent` из `match_player.rating_delta`. **Описание:** сумма взвешенных свежестью знаковых
дельт — «в форме / не в форме».

### 2.3 Прогноз P3 (одиночный)

```
R_eff_A = R_A + k · Form_A
R_eff_B = R_B + k · Form_B
P_model = 1 / (1 + 10^(-(R_eff_A - R_eff_B) / 100))     # логистика, масштаб 100
P_h2h   = (W_A + 1) / (W_A + W_B + 2)                    # сглаживание Лапласа
w       = S / (S + S_ref)
P       = (1 - w) · P_model + w · P_h2h
```

**Константы:** `k`, `S_ref` (и `H` внутри `Form`/`S`). Масштаб `100` — из официальной шкалы (§1.1),
**не параметр**. **Локализация:** `ForecastService` (`core.metrics`, **реализовано, этап 3**),
результат — `ForecastResult`. **Описание:** смесь модельной вероятности (по эффективному рейтингу) и
личной истории; вес смеси растёт со сыгранностью `S`.

### 2.4 Парный прогноз

```
R_пара_прогноз = (A + B) / 2 + Bmax · (1 - 0.5^(S_partner / S0))
Form_пара      = среднее индивидуальных парных форм партнёров
```

Далее — та же логистика и смешивание с `P_h2h`, что в §2.3.

**Константы:** `Bmax`, `S0` (плюс `k`, `S_ref`, `H`). **Локализация:**
`PairRatingService#pairRatingForForecast` + `ForecastService` (`core.metrics`, **реализовано,
этап 3**). **Описание:** к среднему рейтингу пары добавляется бонус за сыгранность партнёров,
насыщающийся к `Bmax`.

### 2.5 Score совместимости партнёра (0–100)

```
C_limit    = min(C_pair, C_player)
C_pair     = min(1, ((R_A + R_B) / 2) / limit_pair)   # limit_pair — лимит среднего рейтинга пары
C_player   = min(1, min(R_A, R_B) / limit_player)     # limit_player — макс. рейтинг одного игрока; NULL → 1
Δ_joint_w  = Σ_i Δ_i · w(Δt_i)                     # Δ_i — суммарная дельта обоих на i-м совместном турнире;
                                                   # w(Δt) — та же кривая свежести, что §2.1–2.2 (H, W_max, α)
C_delta    = sigmoid(Δ_joint_w / D_scale)          # если Δ_joint_w ≤ 0 → C_delta = 0
C_S        = S_partner / (S_partner + S_ref_partner)
C_form     = см. ниже (§2.2, Form кандидата); нет матчей → 0
C_accent   = sigmoid(δ_cat / D_scale)              # если δ_cat ≤ 0 → C_accent = 0

δ_cat — взвешенная средняя δ (§2.7, как `Strength(type)`) по матчам **типа разряда турнира**
(MD/WD/XD), без привязки к «рекомендуемой» категории на карточке; нет матчей этого типа → δ_cat = 0.

C_form:
  Form_cand > 0:  Form_eff = Form_cand · k_stab(зона §2.8); затем (sigmoid(Form_eff / F_form) − 0.5) / 0.5, clamp [0, 1]
  Form_cand < 0:  −min(1, |Form_cand| / F_form)   # стабильность не применяется
  иначе:          0
  нет зоны стабильности при Form > 0: k_stab = 1.0

score = clamp(100 · (w1·C_limit + w2·C_delta + w3·C_S + w4·C_form + w5·C_accent), 0, 100)
```

Для «Новых кандидатов» без совместной истории: `C_delta = 0`, `C_S = 0` (score ≈ от `C_limit`, `C_form`, `C_accent`).

**Рейтинги `R_A`, `R_B` и SQL-пул кандидатов:** всегда парная шкала **`D`** (на ЛАБ нет отдельных MD/WD/XD в
`player_rating`). Тип турнира MD/WD/XD задаёт пол партнёра и (отдельно) игровой акцент, не второй рейтинг.

**Константы:** `w1`…`w5`, `D_scale`, `S_ref_partner`, `F_form`, `T` (окно для сортировки «успешной истории» в bot).
**Локализация:** `PartnerScoreService` (`core.metrics`); `δ_cat` — `GameAccentService#avgWeightedDeltaForType`,
`PlayerGameAccentService#avgDeltaForComposition` (bot).

**Порядок на экране (bot):** score ↓, при равенстве — успешная совместная история за `T` ↓, парный рейтинг ↓;
совпадение категории акцента с турниром влияет на score (`C_accent`) и ⭐, но отдельным ключом сортировки не является.

### 2.7 Игровой акцент (карточка игрока)

Две оси по парным матчам **MD/WD/XD** (одиночки вне акцента). Тип пары — по полу игрока и
партнёра на одной стороне (`PairCompositionService`); `UNKNOWN` не участвует.

**Вес матча** — та же кривая `w(Δt)`, что §2.1–2.2, но с **отдельными** константами
`H_accent`, `W_max_accent`, `α_accent` (дефолты = Form). **Жёсткого отсечения по времени нет**;
число игр на экране — только за последние `display_window` дней (дефолт 180).

**Ось 1 — предпочтение:**

```
Pref(type) = Σ w_i   по матчам типа type
share(type) = Pref(type) / Σ Pref(all)
```

Доминирующий тип — `argmax share`. На экране карточки: категория с заглавной буквы и на следующей
строке число игр доминирующего типа за `display_window` (заголовок «Игровой акцент» с 📊; доля
`share` — для последующей аналитики, на карточке не показывается).

**Ось 2 — сильная сторона:**

```
Strength(type) = Σ (δ_i · w_i) / Σ w_i
WinRate(type)  = Σ (outcome_i · w_i) / Σ w_i   по матчам с δ_i ≠ 0
```

`outcome_i = 1` при `δ_i > 0`, `0` при `δ_i < 0`. Доминирующий тип — `argmax Strength`.
На экране карточки (блок «Рекомендуемая категория» с 🧮): только название категории с заглавной
буквы; типичная δ за матч (`Strength(type)`) и винрейт (%) — для последующей аналитики в других меню.

**Порог показа:** `Σ w_i < W_min` → метрика скрыта (дефолт `W_min = 3`).

**Константы:** `H_accent`, `W_max_accent`, `α_accent`, `W_min`, `display_window`.
**Локализация:** `GameAccentService` (`core.metrics`), вход — `GameAccentEvent`;
потребление — `PlayerGameAccentService` → карточка бота.

### 2.8 Стабильность игрока (0–100)

Два слоя: **внутри турнира** (колебание знака сюрпризов) и **между соседними турнирами**
(смена «тональности» с плохого на хороший и обратно). Направление сюрпризов — в **Форме** (§2.2);
здесь только предсказуемость.

**Сюрприз матча:** `|δ_i| > ε`. Парные дисциплины — один `δ` на матч (как §2.2).

**Внутри турнира** `t`:

```
n⁺ — число матчей с δ_i > ε
n⁻ — число матчей с δ_i < −ε

S^within_t = 1                              если n⁺ = 0 или n⁻ = 0
S^within_t = 1 − min(n⁺, n⁻) / max(n⁺, n⁻)  иначе
```

**Тон турнира** (суммарный знаковый сюрприз):

```
D_t = Σ δ_i   по матчам турнира t с |δ_i| > ε
```

**Между турнирами** — переходы `k` по хронологии (`starts_at`), пары `(t_{k−1}, t_k)`:

```
S^between_k = 0     если D_{t_{k−1}} · D_{t_k} < 0
S^between_k = 0.8   если D_{t_{k−1}} = 0  или  D_{t_k} = 0
S^between_k = 1     иначе
```

Нейтральный турнир (`D_t = 0`) — переход «неопределён»: штраф 20% от смены тона (`S = 0.8`).

**Итог:**

```
Stability = 100 · ( Σ_t S^within_t · w(Δt_t) + Σ_k S^between_k · w(Δt_{t_k}) )
            / ( Σ_t w(Δt_t) + Σ_k w(Δt_{t_k}) )
```

`w(Δt)` — §2.1 (общий `H`, `W_max`, `α`). Вес перехода `k` — от даты **более позднего** турнира
`t_k`. Один турнир в выборке → только слагаемые `S^within`. Нет матчей с `|δ| > ε` — метрика не
показывается.

**Константы:** `ε`, `H`, `W_max`, `α`, пороги зон (§2.8.1). **Локализация:** `StabilityService`
(`core.metrics`, **реализовано**), `StabilityLevel`, `PlayerStabilityService` → карточка бота.
Вход — `match_player.rating_delta` + `tournament.starts_at`, группировка по `tournament_id`.
**Описание:** «насколько стабильно выступает на своём уровне»; смена сильности турнира между
событиями не штрафуется, если тон каждого турнира однороден.

#### 2.8.1 Отображение на карточке (emoji-зоны)

Числовой `Stability` **не показываем** — только emoji по порогам (калибровка на слепке r77, ε=11.5, S=0.8):

| Зона | Stability | Emoji | Дефолт порога |
|---|---|---|---|
| 1 — совсем нестабилен | `[0, 70)` | 🔴 | — |
| 2 — нестабилен | `[70, 80)` | 🟡 | `zone2-min = 70` |
| 3 — середина | `[80, 86)` | ⚪ | `zone3-min = 80` |
| 4 — стабилен | `[86, 92)` | 🟢 | `zone4-min = 86` |
| 5 — супер стабилен | `[92, 100]` | 🔥 | `zone5-min = 92` |

**Локализация:** `StabilityLevel#fromScore`, `StabilityZoneMetrics`, `Texts.card()`.
Строка скрыта, если нет матчей с `|δ| > ε`.

---

## 3. Константы и операционные параметры

Таблицы дефолтов метрик (`badminton-lab.metrics.*`), parser/snapshot и калибровка —
**[`FORMULAR-CONFIG.md`](FORMULAR-CONFIG.md)**.

---

## 4. Калибровка

Дефолты метрик — **стартовые плейсхолдеры** (подробнее — [`FORMULAR-CONFIG.md`](FORMULAR-CONFIG.md) §3).
Калибровка на реальных данных — `PLAN.md` v2+, `BRIEF.md` §11. До калибровки менять значения только через конфиг, без правок формул.
